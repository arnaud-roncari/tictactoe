/// Central registry of all local storage keys.
///
/// Never use raw strings for storage access — always reference this class.
abstract final class StorageKeys {
  static const String startingPlayer = 'starting_player';
}
